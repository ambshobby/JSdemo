export class welcomeconcat 
{
    constructor(name) {
        this.name = name;
    }

    // Method to generate the welcome message
    generateMessage() {
        return `Welcome, ${this.name+'rrrrr'}!!!!!!`;
    } 
    renderring()
    {
        return "I am beign rendered "
    }
}